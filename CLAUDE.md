# CLAUDE.md

## プロジェクト概要

- プロジェクト名: quiz-app
- 種別: 一般常識クイズアプリ
- 技術スタック: HTML / CSS / JavaScript
- 目的: ユーザーがブラウザ上で簡単にプレイできる一般常識クイズを提供し、回答結果とスコアを確認できるアプリを構築する

## 目的と期待される動作

- トップページからクイズを開始できる
- 問題が順番に表示される
- 選択肢を選ぶと正解・不正解を判定する
- スコアや正答数を表示する
- 最終結果画面で再挑戦できる
- シンプルで軽快なブラウザアプリとして動作する

## 技術方針

### 実装方針

- HTML で構造を定義する
- CSS で視認性が高く、モバイル対応も考慮した見た目を作る
- JavaScript で問題データの管理、回答判定、スコア計算、画面遷移を制御する
- 可能な限りシンプルな構成にし、外部依存を最小限にする

### 推奨構成

```text
quiz-app/
├── index.html
├── styles.css
├── script.js
├── README.md
└── assets/        # 必要に応じて画像・アイコンなどを格納
```

- `index.html`: アプリの構造
- `styles.css`: レイアウト、色、ボタン、レスポンシブ対応
- `script.js`: 問題データ、判定ロジック、画面更新
- `assets/`: 自作またはライセンスが明確なメディアのみ格納

## コーディング規約

- JavaScript は ES6 以降の構文を使う
- 変数名は意味が伝わる命名にする
- 関数は単一責務を持たせる
- 冗長な重複は避け、再利用可能な処理をまとめる
- UI の更新は DOM 操作を中心に行う
- エラー処理を簡潔に行い、ユーザーにわかりやすいフィードバックを出す
- 画面表示はシンプルかつ直感的にする

## 品質に関する指針

- 動作確認をブラウザで必ず行う
- スマートフォンでも見やすい設計にする
- 一貫した配色と余白を保つ
- 文字サイズやクリック領域が使いやすいように調整する
- 画面が崩れないように CSS の柔軟性を確保する

## 問題データの扱い

- クイズ問題は自分で作成したオリジナルの内容を使用する
- 第三者のサイトや書籍、画像、文章をそのままコピーしない
- 外部出典を使う場合は、ライセンスが明確で、引用許可や再利用条件が整っているもののみを利用する
- 著作権の不明な画像、音声、文章、ロゴは使わない
- 生成 AI を使う場合も、著作権や利用規約を確認し、著作権侵害の恐れがある内容は避ける

## 著作権・ライセンスに関する重要ルール

- 既存のクイズ、文章、画像、アイコン、音楽、ロゴを無断で使用しない
- 既存のデザインやコードをそのまま流用しない
- 参考にする場合でも、原則として自分のコードに置き換えて実装する
- 画像や音声の使用が必要な場合は、パブリックドメイン、CC0、または明確な使用許諾があるもののみを利用する
- 「見た目の参考」や「インスピレーション」でも、コピーした実装や表現をそのまま再利用しない

## 実装時の推奨ポイント

- 問題を配列で管理し、`shuffle` や `currentQuestionIndex` を使って流れを制御する
- 正解・不正解の状態管理を明確にする
- タイマーや進捗表示などを追加する場合は、UX を損ねない範囲で実装する
- 画面遷移のロジックはわかりやすく分離する

## 完了条件

以下を満たす状態を目標とする:

- アプリがブラウザで正常に起動する
- 問題が表示され、選択肢を選ぶと正解判定が行われる
- 最終結果が表示される
- スコアが正しく計算される
- コードとデータがオリジナルであり、著作権侵害を避けている

## 追加メモ

- このプロジェクトは小規模な教育・娯楽用途の HTML/CSS/JavaScript アプリとして設計する
- 依存ライブラリは必要最小限に抑え、シンプルな構造を優先する
- 実装中は、コピー元のコンテンツや既存の作品を再利用しないことを常に意識する

## GitHubリポジトリ
https://github.com/S-Yamada-prv/quiz-app
